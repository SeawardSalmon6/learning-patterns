{
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "name": "Typescript Debug",
      "preLaunchTask": "tsc: build - tsconfig.json"
    }
  ]
}
